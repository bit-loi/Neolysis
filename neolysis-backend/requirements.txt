# =============================================
# Neolysis Backend — Python Dependencies
# =============================================

# ── FastAPI Core ──────────────────────────────
fastapi
uvicorn[standard]
pydantic
pydantic-settings
python-multipart
sse-starlette

# ── Database (PostgreSQL via Supabase, native asyncpg — NOT Supabase SDK) ──
sqlalchemy>=2.0.0
asyncpg
psycopg2-binary
alembic

# ── Caching ───────────────────────────────────
redis

# ── Cheminformatics ───────────────────────────
rdkit
biopython
numpy
pandas

# ── RAG / Vector Store ────────────────────────
qdrant-client
sentence-transformers
transformers
torch

# ── ADMET Prediction ──────────────────────────
# PyTDC: Therapeutics Data Commons (pretrained ADMET models)
PyTDC
# DeepPurpose: model loader for TDC pretrained models
# deepchem  # Optional: heavyweight, comment out if not needed

# ── LLM / AI ──────────────────────────────────
google-genai
tenacity

# ── HTTP Clients ──────────────────────────────
httpx
requests

# ── Authentication & Security ─────────────────
passlib[bcrypt]
PyJWT
email-validator

# ── Rate Limiting ─────────────────────────────
slowapi

# ── Background Workers ────────────────────────
celery

# ── Environment & Logging ─────────────────────
python-dotenv
loguru

# ── Testing (TDD) ─────────────────────────────
pytest
pytest-asyncio
pytest-mock
httpx    # AsyncClient for FastAPI test client

# ── Dev Tools ─────────────────────────────────
# uvicorn is already included above via uvicorn[standard]
