# Copy to .env. Never commit a real HF_TOKEN.

PLM_MODEL=facebook/esm2_t30_150M_UR50D
PLM_DEVICE=auto
PLM_MAX_RESIDUES=1022
PLM_POOLING=mean

# Optional. Public ESM2 checkpoints do not require a token to download; set this
# only if you switch to a gated/private Hugging Face model.
HF_TOKEN=
